import '@/canvas';
import '@/init';


// import Game from "./game";


// const shadowRenderer = new ShadowRenderer(camera, ctx);

/* const render = () => {
    ctx.clearRect(0, 0, canvas.width, canvas.height);
    ctx.fillStyle = 'rgba(0, 100, 0, 1)';
    ctx.fillRect(0, 0, canvas.width, canvas.height);

    // trail.update();
    // trail.draw(camera);

    if (input.w) player.position.y -= 5;
    if (input.s) player.position.y += 5;
    if (input.a) player.position.x -= 5;
    if (input.d) player.position.x += 5;

    bullet.draw(camera); */
    /* ctx.save();
    ctx.lineWidth = 4;
    ctx.strokeStyle = `hsl(${360 * Math.random()}, 50%, 50%)`
    ctx.beginPath();
    ctx.moveTo(300 + 0, 300);
    ctx.lineTo(300 + 200, 300);
    ctx.stroke();
    ctx.restore();

    ctx.save();
    ctx.lineWidth = 4;
    ctx.strokeStyle = `hsl(${360 * Math.random()}, 50%, 50%)`
    ctx.beginPath();
    ctx.moveTo(300 + 200, 300);
    ctx.lineTo(300 + 150, 300);
    ctx.stroke();
    ctx.restore(); */
    
    /* player.draw(camera, ctx);
    player2.draw(camera, ctx);
    shadowRenderer.renderer([
        { position: player.position, radius: player.lightRadius, type: 'circle' },
        { position: new Vec2({ x: 0, y: 0 }), radius: player.lightRadius, type: 'circle' },
        { position: player.position, distance: 500, angle: new Vec2(ctx.canvas.width / 2, ctx.canvas.height / 2).angle(input.mouse), scattering: Math.PI / 4, type: 'conic' }
    ], 1);
    

    requestAnimationFrame(render);
} */
// const game = new Game();


